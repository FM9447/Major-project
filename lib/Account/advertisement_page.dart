import 'package:flutter/material.dart';

class AdvertisementsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Advertisements'),
      ),
      body: Center(
        child: Text('Advertisements Page'),
      ),
    );
  }
}
